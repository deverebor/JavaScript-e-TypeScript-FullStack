//const nomes = new Array('Lucas', 'Souza', 'Kortoh') construtrutores de arrays.
//delete nomes[2]  remove elemento do array sem alterar os indices.
//const remove = novoNome.pop() armazena o ultimo elemento do array e remove ele.
//novoNome.pop()  remove o ultimo elemento do array.
//console.log(novoNome.length) // tamanho do array.
//const remove = novoNome.shift() // remove o primeiro elemento do array.
//nomes.push('Kiriku') // adiciona elemento no final do array.
//nomes.unshift('Kiriku') // adiciona elemento no inicio do array.
//nomes.splice(1,2, 'Kiriku') // remove elemento do array dinamicamente.
//const novoNome = nomes.slice(1, 3) // cria um novo array a partir de um intervalo de indices.
//const nomes = nome.join(' ') // transforma o array em uma string.

// const nomes = ['Lucas', 'Souza', 'Kortoh', 'Geise', 'Wilson'] //array literal.
// const novoNome = nomes.slice(1, 3) // cria um novo array a partir de um intervalo de indices.


// console.log(novoNome) // tamanho do array.

const nome = ['Lucas', 'Pereira', 'Souza']
const nomes = nome.join(' ') // transforma o array em uma string.

console.log(nomes)